import fs from "node:fs";
import path from "node:path";

export function findImports(entrypoint: string, templatePath: string) {
	console.log(entrypoint, templatePath);
	// const imports: string[] = [];
	// for (const file of fs.readdirSync(templatePath)) {
	//   console.log(file);
	// }
}
